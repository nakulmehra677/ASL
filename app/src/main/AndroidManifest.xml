<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.nakulmehra.hp.asl">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name="com.nakulmehra.hp.asl.Activities.AboutASLActivity"
            android:label="About ASL" />
        <activity android:name="com.nakulmehra.hp.asl.Activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data android:name="android.app.searchable"
                android:resource="@xml/search"/>
        </activity>
        <activity
            android:name="com.nakulmehra.hp.asl.Activities.ChapterListActivity"
            android:label="Chapter list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="com.nakulmehra.hp.asl.Activities.ChapterDetailActivity"
            android:label="Details" />
    </application>

</manifest>